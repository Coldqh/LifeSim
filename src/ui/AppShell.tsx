export function AppShell() {
  return (
    <main className="app-shell">
      <section className="app-card" aria-labelledby="app-title">
        <p className="app-card__eyebrow">Architecture baseline</p>
        <h1 className="app-card__title" id="app-title">
          LifeSim
        </h1>
        <p className="app-card__text">
          Project boundaries are ready. Gameplay systems and city content are intentionally not implemented in this patch.
        </p>
      </section>
    </main>
  );
}
